# 分镜多场景显示修复

## 问题说明
系统之前只显示一个分镜，而需要根据故事的短句数量显示多个分镜。

## 解决方案

### 1. 改进提示词（`_build_storyboard_prompt`）
- **动态计算目标分镜数**：根据故事的句子数量动态确定应生成的分镜数
  - 短故事（≤3个句子）：要求3-4个分镜
  - 中等故事（4-6个句子）：要求4-5个分镜
  - 长故事（>6个句子）：要求5-8个分镜

- **强化提示**：
  - 明确说明分镜数量要求
  - 强调"不允许只生成一个分镜"
  - 在提示结尾加入：`输出数组必须恰好包含 {target_scenes} 个对象`
  - 指导LLM从开头、发展、转折、结尾均要有对应分镜

### 2. 改进段落规划（`_plan_storyboard_segments`）
- **预先计算最少分镜数**：在调用LLM前确定min_scenes值
- **LLM输出验证**：
  - 如果LLM生成的分镜少于最少值，自动从拆分文本中补充
  - 比较文本相似度，避免重复添加

- **自动填充**：
  - 如果补充后仍不足，通过重复使用已有分镜并标注为"补充场景"
  - 确保最后返回的分镜数≥最少要求

- **参数优化**：
  - `max_tokens`：800 → 1000（给LLM更多空间）
  - `temperature`：0.2 → 0.3（提高创意度）

### 3. 前端显示
- **无需修改**：前端app.js已支持显示序列中的所有分镜
- **CSS布局**：已使用自适应网格`grid-template-columns: repeat(auto-fill, minmax(128px, 1fr))`
- **动画效果**：每个分镜有渐入动画，间隔150ms依次显示

## 使用效果

### 规则-基础模式（use_ai=false）
```
短故事 "女孩看着天空。泪水滑落。"
→ 3-4个分镜
```

### AI增强模式（use_ai=true）
LLM会按照提示生成对应数量的分镜，若不足会自动补充

## 测试方式

运行测试脚本验证修复：
```bash
python test_storyboard_fix.py
```

## 修改的文件
- `app.py` - 后端逻辑
  - 第281-314行：`_build_storyboard_prompt()`
  - 第340-437行：`_plan_storyboard_segments()`

## 预期结果
- ✅ 短故事现在显示3-4个分镜
- ✅ 中等故事显示4-5个分镜
- ✅ 长故事显示5-8个分镜
- ✅ 前端网格布局自动适应多个分镜
- ✅ 每个分镜带有渐入动画效果
